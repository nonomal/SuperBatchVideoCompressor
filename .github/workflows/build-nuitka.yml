name: Build with Nuitka

on:
  push:
    tags:
      - 'v*'
    branches:
      - main
      - master
      - dev
    paths:
      - 'src/**'
      - 'cli.py'
      - 'main.py'
      - 'requirements.txt'
  pull_request:
    paths:
      - 'src/**'
      - '*.py'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag for release (e.g., v2.0.0)'
        required: false
        default: ''

env:
  APP_NAME: SBVC
  PYTHON_VERSION: '3.11'

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            artifact_name: SBVC-nuitka-linux-amd64
            asset_name: SBVC-nuitka-linux-amd64
          - os: ubuntu-24.04-arm
            artifact_name: SBVC-nuitka-linux-arm64
            asset_name: SBVC-nuitka-linux-arm64
          - os: macos-15
            artifact_name: SBVC-nuitka-macos-arm64
            asset_name: SBVC-nuitka-macos-arm64
          - os: macos-15-intel
            artifact_name: SBVC-nuitka-macos-amd64
            asset_name: SBVC-nuitka-macos-amd64
          - os: windows-2025
            artifact_name: SBVC-nuitka-windows-amd64.exe
            asset_name: SBVC-nuitka-windows-amd64.exe

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install nuitka ordered-set zstandard

      - name: Install C compiler (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential patchelf

      - name: Install C compiler (macOS)
        if: runner.os == 'macOS'
        run: |
          xcode-select --install || true

      - name: Verify CLI
        run: |
          python cli.py --help

      - name: Build with Nuitka (Unix)
        if: runner.os != 'Windows'
        run: |
          python -m nuitka \
            --onefile \
            --standalone \
            --assume-yes-for-downloads \
            --output-filename=${{ env.APP_NAME }} \
            --output-dir=dist \
            cli.py

      - name: Build with Nuitka (Windows)
        if: runner.os == 'Windows'
        run: |
          python -m nuitka --onefile --standalone --assume-yes-for-downloads --output-filename=${{ env.APP_NAME }}.exe --output-dir=dist cli.py

      - name: Rename artifact (Unix)
        if: runner.os != 'Windows'
        run: |
          mv dist/${{ env.APP_NAME }} dist/${{ matrix.artifact_name }}

      - name: Rename artifact (Windows)
        if: runner.os == 'Windows'
        run: |
          move dist\${{ env.APP_NAME }}.exe dist\${{ matrix.artifact_name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: dist/${{ matrix.artifact_name }}
          retention-days: 30

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Display artifacts
        run: find artifacts/ -type f

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ env.APP_NAME }} ${{ github.ref_name }} (Nuitka)
          body: |
            ## ${{ env.APP_NAME }} ${{ github.ref_name }} (Nuitka Build)

            Super Batch Video Compressor - 超级批量视频压缩器

            ### Downloads
            - **Linux (x64)**: `SBVC-nuitka-linux-amd64`
            - **Linux (ARM64)**: `SBVC-nuitka-linux-arm64`
            - **macOS (Apple Silicon)**: `SBVC-nuitka-macos-arm64`
            - **macOS (Intel)**: `SBVC-nuitka-macos-amd64`
            - **Windows (x64)**: `SBVC-nuitka-windows-amd64.exe`

            ### Requirements
            - FFmpeg must be installed and available in PATH

            ### Build Info
            - Builder: Nuitka (compiled to native code)
            - Python: ${{ env.PYTHON_VERSION }}

            ### Note
            Nuitka builds are compiled to native machine code, offering better startup time and runtime performance compared to PyInstaller bundles.
          files: |
            artifacts/**/*
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
